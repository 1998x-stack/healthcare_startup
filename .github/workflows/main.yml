name: Main

on:
  push:
    branches:
      - main

jobs:
  run-code:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run Python scripts
      run: |
        python ../../medician_copy.py 
        python ../../hospital_info_copy.py
